<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kessr.saphackathon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeNav"/>
        <activity android:name=".CreateAccount"/>
        <activity android:name=".WeatherInformation"/>
        <activity android:name=".CreateEvent"/>
        <activity android:name=".MainFeedPage"/>
        <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity android:name=".AreYouDriver" />
        <activity android:name=".NeedRide" />
        <activity android:name=".AmDriver" />
        <activity android:name=".CarPoolEvent" />
        <activity android:name=".SocialEvent" />
        <activity android:name=".HelpDecisionMaking" />
        <activity android:name=".UserEvents" />
        <activity
            android:name=".CreateSocialEvents"
            android:label="@string/title_activity_home_nav"
            android:theme="@style/AppTheme" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>